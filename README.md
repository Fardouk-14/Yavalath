# Yavalath

An implementation of Yavalath game

## Requirements

Install python 3.10 at least

You'll need matplotlib and torch, you can install it with `pip install matplotlib torch`.

## How to play

Just launch `python Play.py`.
